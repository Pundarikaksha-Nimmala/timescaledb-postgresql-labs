-- ===========================================
-- Lock Monitoring
-- ===========================================

------------------------------------------------
-- Session 1
------------------------------------------------

BEGIN;

UPDATE account
SET balance = balance + 100
WHERE id = 1;

------------------------------------------------
-- Session 2
------------------------------------------------

BEGIN;

UPDATE account
SET balance = balance - 100
WHERE id = 1;

------------------------------------------------
-- Active Sessions
------------------------------------------------

SELECT
    pid,
    usename,
    state,
    wait_event_type,
    wait_event,
    query
FROM pg_stat_activity;

------------------------------------------------
-- Locks
------------------------------------------------

SELECT *
FROM pg_locks;
